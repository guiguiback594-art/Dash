# Dash
on_enabled 0  # --- 2. REDUÇÃO DE RECUO (ESTABILIZAÇÃO) --- settings put system movement_filter_weight 10 settings put system RegisterHit_MinMovePixel 1 echo "[+] Estabilização de recuo aplicada."  # --- 3. OTIMIZAÇÃO TOUCH (BOTÃO 4 / SENSIBILIDADE) --- setprop touch.deviceType touchScreen setprop touch.orientationAware 1 setprop win
